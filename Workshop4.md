# AI Log - Workshop 4

## รายการที่ 1

| หัวข้อ | คำตอบ                                                                                                                                                           |
| --- |-----------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Prompt ที่ใช้ (สรุป) | สั่งให้ AI เขียน Ktor API ตาม Requirement: สร้าง Data class `Task` และ `TaskRequest` (@Serializable), สร้าง `TaskRepository` (In-Memory), และเขียน Routes (GET, POST, PUT, DELETE) พร้อมกำหนด Status Code |
| AI ตอบผิด / น่าสงสัยตรงไหน | โค้ดที่ AI เจนออกมาในส่วนของ `POST /tasks` อาจมีความขัดแย้งกับโจทย์ เพราะโจทย์สั่งว่าให้ใช้ `call.receive<Task>()` แต่ในทางปฏิบัติ ตอนสร้าง Task ใหม่เรายังไม่มี ID เราจึงควรรับค่าผ่าน `TaskRequest` มากกว่า |
| เราตัดสินใจ / แก้อย่างไร | ตรวจสอบและปรับแก้โค้ดในส่วน POST ให้รับข้อมูลเป็น `call.receive<TaskRequest>()` แทน เพื่อรับแค่ `content` กับ `isDone` จากนั้นให้ระบบจัดการสร้าง ID ใหม่ให้เอง แล้วค่อยแปลงเป็น `Task` เพื่อเซฟลง Repository |
| สิ่งที่ได้เรียนรู้ | ได้เข้าใจหลักการทำงานของ REST API เบื้องต้น การแยกระหว่าง Model ที่ใช้เก็บข้อมูล (`Task`) กับ Model ที่ใช้รับ Request (`TaskRequest`) และการเลือกใช้ HTTP Status Code ให้เหมาะสมกับสถานการณ์ (200 OK, 201 Created, 404 Not Found) |

## รายการที่ 2

| หัวข้อ | คำตอบ                                                                                                                                                                                                     |
| --- |-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Prompt ที่ใช้ (สรุป) | ขอวิธีทดสอบ Endpoint ทั้งหมดที่สร้างขึ้นมา โดยให้ AI แนะนำคำสั่ง Terminal สำหรับใช้บนระบบปฏิบัติการ Windows |
| AI ตอบผิด / น่าสงสัยตรงไหน | AI ให้คำสั่งมาถูกต้อง (ใช้ `Invoke-RestMethod` หรือ `curl.exe`) แต่ไม่ได้เตือนว่าบางครั้งใน Windows CMD การใส่เครื่องหมายอัญประกาศ (`"`) ใน JSON string อาจเกิด Error ได้ง่ายถ้า Escape string ไม่ดี |
| เราตัดสินใจ / แก้อย่างไร | เลือกทดสอบผ่าน PowerShell โดยใช้คำสั่ง `Invoke-RestMethod` ตามที่ AI แนะนำ เพราะจัดการกับการส่ง Body แบบ JSON ได้ง่ายกว่า และทดสอบยิง Request เรียงตามลำดับ: POST สร้าง -> GET ดูข้อมูล -> PUT แก้ไข -> DELETE ลบ |
| สิ่งที่ได้เรียนรู้ | ได้เรียนรู้วิธีทดสอบ API ด้วย Command Line Tool บน Windows โดยไม่ต้องพึ่งพาโปรแกรม UI อย่าง Postman ทำให้เข้าใจกลไกการส่ง Headers และ Body ใน HTTP Request มากขึ้น |